Imscribing/Primitives/Lattice.lean — PartialOrder Criticality proved; README updated
=======================================================================

Changes:
  Imscribing/Primitives/Lattice.lean
    - Fixed crit_meet_absorb_complex_left/right: added (h : x ≠ Phi_c) hypothesis —
      arm 2 (_, Phi_c => Phi_c) fires before arm 3, so Phi_c_complex absorption
      only holds when the other element is not Phi_c
    - Added import Mathlib.Data.Fintype.Basic (not pulled in by Mathlib.Order.Lattice)
    - Added private Fintype Criticality instance
    - Added private instDecidableLTCriticality: Decidable (a < b) via
      inferInstanceAs (Decidable (compare a b = .lt)) — required for decide on
      lt_iff_le_not_ge
    - Proved full PartialOrder Criticality by revert … ; decide over the Fintype;
      replaced prior simp_all / cases approach that could not reduce deriving Ord compare
    - Fixed lt_iff_le_not_ge field name (was lt_iff_le_not_le, not a PartialOrder field)
    - Fixed long line in absorption_is_not_semilattice_inf_complex
    - Removed dead Fintype + crit_cmp_table machinery; removed decide-over-bool approach
    - Removed duplicate Toolchain line from README header
    Build: ✔ [614/614] Built Imscribing.Primitives.Lattice — zero errors, warnings only
    in Core.lean (pre-existing whitespace lints)

  README.md
    - Added Primitives/Lattice.lean section (Library Reference, after Core.lean)
    - Documented crit_meet absorption semantics and the ≠ Phi_c side condition
    - Documented PartialOrder proof strategy (Fintype + instDecidableLTCriticality)
    - Added three proof-engineering notes: Fintype import, Decidable (a < b) pattern,
      simp_all vs decide on deriving Ord compare
