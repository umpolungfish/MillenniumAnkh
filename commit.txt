Imscribing Paraconsistent layer: Lattice, QCI fixes, DialetheicAlignment, Shor pipeline
=======================================================================

Imscribing/Primitives/Lattice.lean — PartialOrder Criticality proved; README updated
──────────────────────────────────────────────────────────────────────────────────────
  - Fixed crit_meet_absorb_complex_left/right: added (h : x ≠ Phi_c) hypothesis —
    arm 2 (_, Phi_c => Phi_c) fires before arm 3, so Phi_c_complex absorption
    only holds when the other element is not Phi_c
  - Added import Mathlib.Data.Fintype.Basic (not pulled in by Mathlib.Order.Lattice)
  - Added private Fintype Criticality instance
  - Added private instDecidableLTCriticality: Decidable (a < b) via
    inferInstanceAs (Decidable (compare a b = .lt)) — required for decide on
    lt_iff_le_not_ge
  - Proved full PartialOrder Criticality by revert … ; decide over the Fintype;
    replaced prior simp_all / cases approach that could not reduce deriving Ord compare
  - Fixed lt_iff_le_not_ge field name (was lt_iff_le_not_le, not a PartialOrder field)
  - Fixed long line in absorption_is_not_semilattice_inf_complex
  - Removed dead Fintype + crit_cmp_table machinery; removed decide-over-bool approach
  Build: ✔ [614/614] Built Imscribing.Primitives.Lattice — zero errors, warnings only

README.md
──────────
  - Added Primitives/Lattice.lean section (Library Reference, after Core.lean)
  - Documented crit_meet absorption semantics and the ≠ Phi_c side condition
  - Documented PartialOrder proof strategy (Fintype + instDecidableLTCriticality)
  - Added three proof-engineering notes: Fintype import, Decidable (a < b) pattern,
    simp_all vs decide on deriving Ord compare

Imscribing/Paraconsistent/QCI_PvsNP_Bridge.lean
────────────────────────────────────────────────
  - Fixed joinCircuit: replaced invalid Finset.univ.fold with List.finRange + List.foldl
  - Fixed join_circuit_B_dominant statement to reference joinCircuit n v
  - Fixed classical_cannot_become_B: replaced rcases rfl pattern with
    rcases h with h | h <;> rw [h] <;> decide via measure_nonsuper_idempotent

Imscribing/Paraconsistent/QCI_SICPOVM_Bridge.lean
──────────────────────────────────────────────────
  - Added import Mathlib.Data.Fintype.Basic
  - Added private Fintype Belnap instance
  - Rewrote belnapToWH2 using (0 : Fin 2) / (1 : Fin 2) OfNat literals (not ⟨n, by norm_num⟩)
  - belnapToWH2_injective: by decide (requires Fintype Belnap)
  - belnapToWH2_bijective: ⟨injective, fun ⟨a, b⟩ => by revert a b; decide⟩
  - Removed belnap_card_eq_wh2_card (Fintype.card not available)
  - Fixed B_is_maximal_displacement / N_is_identity: ((1 : Fin 2), (1 : Fin 2)) notation
  - Fixed belnap_nonB_equidistant 4th conjunct: ¬ (N ≤ T) → ¬ (F ≤ N)
    (N is bottom via n_bot; original statement was false)

Imscribing/Paraconsistent/DialetheicAlignment.lean — new file (added via lakefile)
───────────────────────────────────────────────────────────────────────────────────
  - Fixed floating /-- doc comment → /- plain block comment
  - Fixed kernel_classical_hamming_exact: claim = 7 is wrong; changed to ≥ 7 with simp

Imscribing/Paraconsistent/Shor/{BelnapModExp,BelnapQFT,FullPipeline}.lean — new files
───────────────────────────────────────────────────────────────────────────────────────
  BelnapModExp.lean
    - allBWord, ModExpResult, shor15_7, ratio_invariant
    - Key: band B B = B → B propagates through all Boolean gates, cost 0
  BelnapQFT.lean
    - qftCoherenceCost, phi_upsilon_bottleneck
    - Finding: Belnap QFT on all-B is identity; no phase differentiation
  FullPipeline.lean
    - NQState n-register, shorPipelineImscription, shor_pipeline_tier, coherence_ratio_is_two
    - Pipeline: H^⊗n (cost n) → ModExp (cost 0) → B-bias (cost 2n) → T-bias (cost n)
    - Invariant: 2n/n = 2 (always); Φ_υ bottleneck documented

Imscribing/Paraconsistent/Shor/belnap_shor_executor.py — new file
────────────────────────────────────────────────────────────────────
  Full Python executor verifying all Lean pipeline invariants.
  Wigner's Friend coherence demo, SIC-POVM axiom check, three Shor instances.
  All assertions pass; Φ_υ bottleneck demonstrated.

lakefile.toml
─────────────
  - Added entries: DialetheicAlignment, Shor.BelnapModExp, Shor.BelnapQFT, Shor.FullPipeline
